

allow '/' tolerance for urls and config file
option to make server slower (for siege tests)

format code in unified style
clean-up comments
remove DEBUG logs
makefile: remove -g and -fsanitize

consider putting everything inside a webserv singleton
